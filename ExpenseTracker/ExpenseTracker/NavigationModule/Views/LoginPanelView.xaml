<UserControl x:Class="ExpenseTracker.NavigationModule.Views.LoginPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="180">
    <Grid>
        <StackPanel>
            <Button Name="btnCreateNewAccount" Width="180" Height="60" Content="Create New Account" FontFamily="Times New Roman" FontSize="20" FontWeight="Bold" Command="{Binding Path=NavigateCreateNewAccountCommand}"/>
            <Separator Foreground="Blue" Background="Blue" Margin="0,4,0,4"/>
            <Button Name="btnLogin" Width="180" Height="60" Content="Login" FontFamily="Times New Roman" FontSize="20" FontWeight="Bold" Command="{Binding Path=NavigateLoginCommand}"/>
            <Separator Foreground="Blue" Background="Blue" Margin="0,4,0,4"/>
            <Button Name="btnExit" Width="180" Height="60" Content="Exit Application" FontFamily="Times New Roman" FontSize="20" FontWeight="Bold" Command="{Binding Path=ExitCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
